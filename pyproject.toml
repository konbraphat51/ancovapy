[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ancovapy"
version = "0.1.0"
description = "A Python library for ANCOVA and Bayesian ANCOVA with scientific rigor"
authors = [
    {name = "ancovapy contributors"}
]
readme = "README.md"
requires-python = ">=3.9"
license = {text = "BSL-1.0"}
keywords = ["ancova", "statistics", "bayesian", "covariance analysis"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Boost Software License 1.0 (BSL-1.0)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Typing :: Typed",
]

dependencies = [
    "numpy>=1.20.0",
    "scipy>=1.7.0",
    "pandas>=1.3.0",
    "statsmodels>=0.13.0",
    "pymc>=5.0.0",
    "arviz>=0.15.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "mypy>=1.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["ancovapy"]

[tool.mypy]
python_version = "3.9"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = false
warn_no_return = true
# Relax strict settings for untyped third-party libraries
disallow_any_generics = false
disallow_subclassing_any = false
disallow_untyped_calls = false
disallow_incomplete_defs = true
disallow_untyped_decorators = false

# Ignore missing imports for third-party libraries
[[tool.mypy.overrides]]
module = [
    "pandas.*",
    "scipy.*",
    "statsmodels.*",
    "pymc.*",
    "arviz.*",
    "patsy.*",
    "numpy.*",
]
ignore_missing_imports = true

[tool.black]
line-length = 88
target-version = ['py39']

[tool.ruff]
line-length = 88
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
